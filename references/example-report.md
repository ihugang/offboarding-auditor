# 项目继承审计报告：billing-service（订阅计费服务）

> 本文是一份**填好的样板**，供 Claude 生成报告时参照风格与颗粒度。
> 内容为虚构示例，演示「证据驱动 + 接手人视角 + 可执行待补清单」应有的样子。

- 审计对象：`git@github.com:acme/billing-service`（约 1.8 万行 Go）
- 离职负责人：李伟（独立维护 14 个月，最后工作日 +5 天）
- 接手人画像：王芳，后端中级，**全新加入、不懂计费域**
- 审计日期：2026-06-02

## 结论

- **可继承度评分：54 / 100**
- **交接判定：❌ 不可交接 — 存在 2 个阻断项**
- 一句话总结：代码本身质量尚可，但**支付网关密钥与生产部署权限只绑在李伟个人账号上**，且核心对账逻辑零文档——人一走，新人既发不了版也看不懂为什么这么算钱。

## 阻断项 🔴（不解决无法交接）

- [ ] **生产部署密钥绑定个人账号** — 证据：`.github/workflows/deploy.yml:23` 用 secret `LIWEI_GCP_SA`，对应 GCP 服务账号属李伟个人 Google 账号。人离职禁用账号后流水线即失效，**无法发版**。待补：迁移到团队服务账号，更新 CI secret。
- [ ] **Stripe 主密钥来源未知** — 证据：生产 `STRIPE_SECRET_KEY` 不在 `.env.example`，仓库无记录，仅李伟知道在哪个 Stripe 账号下生成。待补：确认 Stripe 账号归属、移交 owner 权限、记录密钥轮换流程。

## 高危项 🟠

- [ ] **对账逻辑无「为什么」** — 证据：`internal/recon/proration.go:88-140` 的按比例退款算法含 3 个魔数（`0.97`、`30`、`86400`），无注释无文档。新人不敢改、改错直接算错钱。待补：李伟补一段 ADR 说明计费规则来源。
- [ ] **核心模块为知识孤岛** — 证据：`inventory.sh` 显示 `internal/recon/` 下 9 个文件唯一作者均为李伟，无 code review 痕迹。
- [ ] **手工运维步骤未记录** — 证据：`rg "手动|manual"` 命中 `cmd/migrate/README` 提到「每月 1 号手动跑一次对账补偿脚本」，但脚本位置、参数、失败处理全无。待补：写成 runbook。

## 维度评分

| 维度 | 评分 | 关键发现 |
|------|------|---------|
| 1 可运行性 | 6/10 | 本地能起，但缺真实密钥（见阻断项 2）；`.env.example` 缺 4 个变量 |
| 2 可理解性 | 4/10 | 有 README 架构图，但核心计费规则零解释 |
| 3 可维护性 | 6/10 | 代码整洁、命名清晰；但 recon 模块是黑盒 |
| 4 可测试性 | 7/10 | 单测覆盖 68%，`make test` 可跑；对账模块缺集成测试 |
| 5 隐性知识 ⚠️ | 3/10 | 魔数、手工补偿步骤、Stripe webhook 重试坑均未文档化 |
| 6 依赖权限归属 ⚠️ | 2/10 | 部署密钥 + Stripe owner 均绑个人账号（两个阻断项） |
| 7 运维与部署 | 4/10 | 有 CI，但回滚流程、告警入口未写 |
| 8 未完成工作 | 5/10 | `feat/usage-based-billing` 分支领先 main 12 提交，半成品，无说明 |
| 9 业务上下文 | 6/10 | 需求来源在 Notion，但链接散落在 PR 描述里 |
| 10 Bus Factor | 2/10 | 全项目仅李伟一人深度了解，无第二知情人 |

## 离职前待补清单（按优先级）

| 优先级 | 待补项 | 对应风险 | 负责人 | 预计工作量 |
|--------|--------|---------|--------|-----------|
| 🔴 | 迁移部署 SA 到团队账号并改 CI secret | 阻断项 1 | 李伟 + 运维 | 3h |
| 🔴 | 移交 Stripe owner，记录密钥来源与轮换 | 阻断项 2 | 李伟 | 2h |
| 🟠 | 写对账规则 ADR（解释 3 个魔数的业务来源） | 隐性知识 | 李伟 | 3h |
| 🟠 | 写月度对账补偿脚本 runbook | 运维 | 李伟 | 2h |
| 🟠 | `feat/usage-based-billing` 分支写状态说明（继续/弃用/阻塞点） | 未完成工作 | 李伟 | 1h |
| 🟡 | 补全 `.env.example` 缺失的 4 个变量 | 可运行性 | 李伟 | 0.5h |
| 🟡 | 回滚流程 + 告警入口写进 README | 运维 | 王芳可后补 | 1h |

> 优先级判据：🔴🟠 全部指向「只有李伟知道、且人走后再也问不到」的知识，必须趁人在补；🟡 中有些（如回滚流程）公开可查，可留给王芳上手时完善。

## 留给新人的「上手 30 分钟」入口

当前文档不足以支撑 30 分钟上手，**这一节本身就是最大缺口**。补齐上方清单后，理想入口应是：
1. `docs/architecture.md` — 看懂三个服务怎么协作
2. `docs/billing-rules.adr.md`（待补）— 理解为什么这么算钱
3. `docs/runbook.md`（待补）— 学会发版、回滚、跑月度对账
4. 本地 `make dev` 起服务，用 Stripe 测试 key 跑通一笔订阅
